{
  "name": "learnthings-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "learnthings-mcp": "./build/index.js"
  },
  "scripts": {
    "clean": "rimraf ./build",
    "build": "npm run clean && tsc",
    "watch": "tsc --watch",
    "start": "node ./build/index.js",
    "bundle": "npm run build && node scripts/bundle.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "core": "file:../core",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "@types/pouchdb": "^6.4.2",
    "rimraf": "^5.0.5",
    "typescript": "~5.9.3"
  }
}
